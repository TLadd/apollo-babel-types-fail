{
  "name": "apollo-babel-types-fail",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@babel/types": "7.0.0-beta.46",
    "apollo": "^1.4.0"
  },
  "scripts": {
    "genFlow": "apollo codegen:generate --schema=schema.json --target=flow",
    "genTs": "apollo codegen:generate --schema=schema.json --target=typescript"
  }
}
